{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Qemu run",
            "type": "shell",
            "command": "cd .. && ./run.sh"
        },

        {
            "label": "Qemu debug",
            "type": "shell",
            "command": "cd .. && ./run.sh -S -s"
        }
    ]
}